import styled from "styled-components";

const StyledShowContextBar = styled.div`
  position: sticky;
  top: 15px;
  display: flex;
  flex-direction: row;
  justify-content: center;
  gap: 0.3rem;
  margin-top: 0.5rem;
  z-index: 3;
`;

export default StyledShowContextBar;
